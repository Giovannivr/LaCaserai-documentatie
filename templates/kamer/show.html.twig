{% extends 'base.html.twig' %}

{% block title %}Kamer{% endblock %}

{% block body %}
    <h1>Kamer</h1>

    <table class="table" style="color: white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kamer.id }}</td>
            </tr>
            <tr>
                <th>Prijs</th>
                <td>{{ kamer.prijs }}</td>
            </tr>
        </tbody>
    </table>
    <div style="display: flex">
        <a class="btn btn-primary" style="margin-right: 10px" href="{{ path('kamer_index') }}">back to list</a>

        <a class="btn btn-secondary" style="margin-right: 10px" href="{{ path('kamer_edit', {'id': kamer.id}) }}">edit</a>

        {{ include('kamer/_delete_form.html.twig') }}
    </div>
{% endblock %}
{% block footer %}
{% endblock %}
