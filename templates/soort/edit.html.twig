{% extends 'base.html.twig' %}

{% block title %}Edit Soort{% endblock %}

{% block body %}
    <h1>Edit Soort</h1>

    {{ include('soort/_form.html.twig', {'button_label': 'Update'}) }}
    <div style="display: flex">
        <a class="btn btn-primary" style="margin-right: 10px" href="{{ path('soort_index') }}">back to list</a>

        {{ include('soort/_delete_form.html.twig') }}
    </div>
{% endblock %}
{% block footer %}
{% endblock %}
