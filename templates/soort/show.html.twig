{% extends 'base.html.twig' %}

{% block title %}Soort{% endblock %}

{% block body %}
    <h1>Soort</h1>

    <table class="table" style="color: white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soort.id }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ soort.omschrijving }}</td>
            </tr>
            <tr>
                <th>Meerprijs</th>
                <td>{{ soort.meerprijs }}</td>
            </tr>
        </tbody>
    </table>
    <div style="display: flex">
        <a class="btn btn-primary" style="margin-right: 10px" href="{{ path('soort_index') }}">back to list</a>

        <a class="btn btn-secondary" style="margin-right: 10px" href="{{ path('soort_edit', {'id': soort.id}) }}">edit</a>

        {{ include('soort/_delete_form.html.twig') }}
    </div>
{% endblock %}
{% block footer %}
{% endblock %}
